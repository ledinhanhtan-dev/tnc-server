import { CalcTotalsInterceptor } from './calc-totals.interceptor';

describe('CalcTotalsInterceptor', () => {
  it('should be defined', () => {
    expect(new CalcTotalsInterceptor()).toBeDefined();
  });
});
